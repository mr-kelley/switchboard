# Dependencies
node_modules/

# Build output
dist/
out/
release/

# Electron
*.asar

# Environment
.env
.env.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Decision log derived index
decisions/index.sqlite

# Coverage
coverage/

# Logs
*.log
npm-debug.log*

# Issue drafts (local working directory)
.issues/
.cache/

# Handoffs — inbound/outbound working documents that typically contain
# lab-specific info; other roles keep authoritative copies in their own
# internal repos. My copy stays untracked.
.handoffs/

# Perf monitor CSVs (see scripts/perf/lib/sample_{cpu,mem}.sh)
sample_cpu_*.csv
sample_mem_*.csv

# Fleet perf-run trees produced by scripts/perf/run_daemon_only.sh — these
# contain host.json with real hostnames. Contribute individual runs via PR
# by copying the sanitized bits into a report, not by committing the tree.
perf-runs/
